var countBits = function(n) {
    let dp = [0];
    let offset = 1;
    for(let i = 1; i<=n; i++) {
        if(i==offset*2) {
            offset*=2;
        }
        dp[i] = 1+dp[i-offset];
    }
    return dp;
};
