var maxProfit = function(prices) {
    let min = Infinity;
    let max = 0;
    
    for(price of prices) {
        if(price<min)  min = price;
        if(price - min > max) max = price - min;
    }
    
    return max;
};
